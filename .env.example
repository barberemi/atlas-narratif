# ── Client (Vite) ─────────────────────────────────────────────────────────────
# URL de l'API backend (utilisée par le frontend pour les requêtes)
VITE_API_URL=http://localhost:3001

# Website ID Crisp (chat widget) — laisser vide pour désactiver
VITE_CRISP_WEBSITE_ID=
