===============================================================================
  MODE COOPÉRATIF + MODÉRATION PUBLIQUE
  Multi-auteurs, rôles, invitations, vue publique, suggestions
===============================================================================

## Phase 1 — Fondations (DB + middleware)
[ ] Migration SQL `server/db/migrations/002_collaboration.sql`
    - ALTER projects ADD visibility TEXT NOT NULL DEFAULT 'private'
    - CREATE TABLE project_members (project_id, user_id, role CHECK owner/editor/viewer, joined_at)
    - CREATE TABLE project_invitations (id, project_id, email, role, invited_by, expires_at, accepted_at)
    - CREATE TABLE suggestions (id, project_id, entity_type, entity_id, field, current_value, proposed_value, author_name, author_email, author_user_id, reason, status, reviewed_by, reviewed_at, review_comment)
    - Rétrofill : INSERT project_members (owner) pour tous les projets existants avec user_id
[ ] Middleware `server/src/middleware/requireProjectAccess.js`
    - Hiérarchie : owner (3) > editor (2) > viewer (1)
    - Lookup project_members pour users authentifiés
    - Fallback projects.user_id pour backward compat
    - Device anonyme : garde le comportement actuel
    - Pose c.set('projectRole', role)
[ ] Réécrire requireProjectOwner comme alias → requireProjectAccess('owner')
[ ] Appliquer les rôles par verbe HTTP dans api.js
    - GET → viewer, POST/PUT → editor, DELETE projet → owner, DELETE entité → editor
    - Routes membres/invitations/settings → owner
[ ] Mettre à jour getProjects() : UNION projets propres + partagés (JOIN project_members)
[ ] INSERT auto project_members(owner) dans createProject() pour users authentifiés
[ ] Tests : vérifier que les projets existants fonctionnent toujours (backward compat)

## Phase 2 — Gestion des membres + invitations
[ ] Routes API membres :
    - GET /projects/:projectId/members (viewer)
    - PUT /projects/:projectId/members/:userId/role (owner)
    - DELETE /projects/:projectId/members/:userId (owner)
    - POST /projects/:projectId/transfer-ownership (owner)
[ ] Routes API invitations :
    - GET /projects/:projectId/invitations (owner)
    - POST /projects/:projectId/invitations (owner) → body: { email, role }
    - DELETE /projects/:projectId/invitations/:id (owner)
    - POST /api/invitations/accept (auth) → body: { token }
    - POST /api/auth/accept-pending-invitations (auth) → auto-accept à la connexion
[ ] Validators Zod : createInvitation, updateMemberRole, transferOwnership
[ ] Extraire sendEmail + emailHtml de auth.js → server/src/email.js
[ ] Email d'invitation : nom projet, invitant, rôle, CTA → /login?invitation={id}
[ ] Auto-accept : à la connexion/inscription, accepter toutes les invitations matching email
[ ] DB queries : getProjectMembers, addProjectMember, updateProjectMemberRole,
    removeProjectMember, transferProjectOwnership, getProjectInvitations,
    createInvitation, deleteInvitation, acceptInvitation, acceptPendingInvitationsForUser
[ ] Store Zustand : src/stores/useMemberStore.js (members, invitations, actions CRUD)
[ ] API client : fonctions membres/invitations dans src/api/client.js
[ ] Page settings : src/pages/ProjectSettingsPage.jsx (onglets : Général, Membres, Visibilité, Modération)
[ ] Composants :
    - src/components/settings/MemberList.jsx (table + rôle + boutons)
    - src/components/settings/InviteForm.jsx (email + sélecteur rôle)
    - src/components/nav/RoleBadge.jsx (badge owner/editor/viewer dans TopNav)
[ ] Route /settings dans App.jsx (derrière RequireProject)
[ ] ProjectPicker : distinguer "Mes projets" / "Partagés avec moi" + afficher rôle
[ ] TopNav : icône settings (visible si owner/editor)
[ ] Hook useCanEdit() : désactiver édition si role === 'viewer'
[ ] Tests : inviter collaborateur → il voit le projet → il peut éditer → viewer ne peut pas

## Phase 3 — Visibilité publique + suggestions
[ ] Routes publiques server/src/routes/public.js (monté AVANT requireIdentity)
    - Middleware requirePublicProject : vérifie visibility = 'public'
    - GET /public/projects/:projectId (métadonnées)
    - GET /public/projects/:projectId/lore, events, stc, volumes, plants, threads, hero-journey, character-arcs
    - POST /public/projects/:projectId/suggestions (rate limited 5/10min/IP)
[ ] Routes modération (editor+) :
    - GET /projects/:projectId/suggestions (filtre status)
    - PUT /projects/:projectId/suggestions/:id/approve (approuver + appliquer)
    - PUT /projects/:projectId/suggestions/:id/reject (rejeter + commentaire)
[ ] Approval flow :
    - Transaction : vérifier entité, UPDATE le champ ciblé, SET status='approved'
    - Allowlist explicite de tables/champs modifiables (SUGGESTION_TARGETS) pour prévenir injection
[ ] Validators Zod : createSuggestion, reviewSuggestion, projectSettings
[ ] DB queries : createSuggestion, getSuggestions, approveSuggestion, rejectSuggestion,
    updateProjectSettings, getProjectSettings
[ ] Store : src/stores/useSuggestionStore.js (suggestions, pendingCount, filter, approve, reject)
[ ] API client : fonctions publiques séparées (fetch sans credentials) + fonctions modération
[ ] Page publique : src/pages/PublicProjectPage.jsx — route /p/:projectId (hors RequireProject)
[ ] Composants publics :
    - src/components/public/PublicLayout.jsx (layout lecture seule)
    - src/components/public/SuggestButton.jsx (bouton sur chaque entité)
    - src/components/public/SuggestionForm.jsx (modal : champ, valeur, nom/email/raison)
[ ] Composants modération :
    - src/components/moderation/SuggestionQueue.jsx (liste filtrable)
    - src/components/moderation/SuggestionCard.jsx (diff current/proposed + approve/reject)
[ ] Settings : onglet Visibilité (toggle public + lien partageable)
[ ] TopNav : badge nombre suggestions en attente
[ ] Tests : toggle public → visiter /p/:projectId incognito → soumettre suggestion → approuver

## Phase 4 — Polish
[ ] Notifications email pour nouvelles suggestions (batch, pas temps réel)
[ ] Bouton "Copier le lien public" dans settings
[ ] Renvoyer une invitation
[ ] Tests E2E complets (Playwright) pour collaboration + modération
[ ] Mise à jour CLAUDE.md (routes, stores, tables)
[ ] Mise à jour ai/docs/ (data-layer.md, routes.md, components.md)
